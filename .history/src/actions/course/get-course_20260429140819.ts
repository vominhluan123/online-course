import { ConnectToDatabase } from "@/lib/db";

export async function getCourse(params: type) {
  try {
    ConnectToDatabase();
    const course = aw
  } catch (error) {
    console.log(error);
  }
}
